<Window x:Class="GolfReview.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GolfReview"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!-- Row 0 -->
        <Menu DockPanel.Dock="Top" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
            <MenuItem Header="File">
                <MenuItem Header="Create"/>
            </MenuItem>
        </Menu>

        <!--Row 2-->
        <Canvas Grid.Row="2" Grid.Column="1" Grid.RowSpan="2" Name="canvasHoleMap" Width="300" Height="500"/>

        <DataGrid Grid.Row="2" Grid.Column="3" Name="dataGridScores" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="Nationality" Binding="{Binding Path=Nationality}"/>
                <DataGridTextColumn Header="1" Binding="{Binding Path=Hole1}"/>
                <DataGridTextColumn Header="2" Binding="{Binding Path=Hole2}"/>
                <DataGridTextColumn Header="3" Binding="{Binding Path=Hole3}"/>
                <DataGridTextColumn Header="4" Binding="{Binding Path=Hole4}"/>
                <DataGridTextColumn Header="5" Binding="{Binding Path=Hole5}"/>
                <DataGridTextColumn Header="6" Binding="{Binding Path=Hole6}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!--Row 3-->
        
        <!-- Row 4 -->


    </Grid>
</Window>
